#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main- prints alphabets in lower case
*
*Return: always (success)
*/
int main(void)
{
char alph[26]

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	(int i = 0 i < 26, i++)
	putchar(lowercase alphabet "a,b,c..z", \n, "a,b,c..z")
	putchar('\n');
	return (0);
}
