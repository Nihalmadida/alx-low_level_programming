#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main- prints alphabets in lower case
*
*Return: always 0 (success)
*/
int main(void)
{
	srand(time(0));
	i = rand() - RAND_MAX / 2;
	char alp[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

if (i = 0; i < 26; i++);
{
	putchar(alp[i]);
}
	putchar('\n');
return (0);
}
